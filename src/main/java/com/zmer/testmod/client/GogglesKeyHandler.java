package com.zmer.testmod.client;

/**
 * Previously handled G-key binding for QTE.
 * QTE is now triggered automatically from WireframeGoggles.canUnequip()
 * when the player tries to remove the goggles from the Curios slot.
 * This class is kept empty to avoid dangling references.
 */
public class GogglesKeyHandler {
    // No longer needed — QTE opens automatically on unequip attempt
}
